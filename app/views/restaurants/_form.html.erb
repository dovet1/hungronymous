<div class="form-text">
  <%= simple_form_for @restaurant do |f| %>
    <%= f.input :name %>
    <%= f.input :stars, collection: 1..5 %>
    <%= f.input :review, as: :text %>
    <%= f.button :submit, "Add review", class: "form-button btn btn-primary"%>
  <% end %>
</div>
